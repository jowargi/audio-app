# 🎧 Наушники API

## 📦 **Продукты (Наушники)**

1. **GET** `/products` – получить список всех наушников
2. **GET** `/product/:productId` – получить наушники по ID  
   Пример: `/product/MLXJ2LLA`

## 🧬 **Кодеки**

1. **GET** `/codecs` – получить все кодеки
2. **GET** `/codecs?productId=:productId` – получить кодеки по ID наушников  
   Пример: `/codecs?productId=MLXJ2LLA`

## 📝 **Отзывы**

1. **GET** `/reviews` – получить все отзывы
2. **GET** `/reviews?productId=:productId` – получить отзывы по ID наушников  
   Пример: `/reviews?productId=MLXJ2LLA`
3. **POST** `/review/:productId` – создать отзыв для наушников  
   Пример: `/review/MLXJ2LLA`  
   Тело запроса (без поля `id`):
   ```json
   {
     "user": "hr83h29h9h9u12h9213",
     "text": "Отличный звук!",
     "rating": 5
   }
   ```
4. **PATCH** `/review/:reviewId` – изменить отзыв по его ID  
   Пример: `/review/asdad312143e3r2re12e`  
   Тело запроса (только обновляемые поля):
   ```json
   {
     "text": "Обновлённый отзыв",
     "rating": 4
   }
   ```

## 👤 **Пользователи**

1. **GET** `/users` – получить всех пользователей

---

### 🆔 Примеры ID для тестирования:

- **Наушники (productId):** `MLXJ2LLA`, `WF1000XM3V`, `M3IEG`, `794418`, `MARSHALLMAJORIVBLK`
- **Пользователи (user):** `hr83h29h9h9u12h9213`, `jg4985gj94`, `asdasdoi29tu384f`
- **Отзывы (reviewId):** `asdad312143e3r2re12e`, `as3121asdad43e3r2re12e`, `aad31214sd2fr2rwede3r2re12e`

---

### 💡 Примечания:

- Все ответы возвращаются с задержкой ~1 секунда (имитация реального API)
- ID генерируются автоматически при создании новых записей
- Для связанных данных (например, отзывов и кодеков наушников) используйте параметр `productId` в query string
